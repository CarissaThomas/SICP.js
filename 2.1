//2.1 
//handles positive and negative arguments 
//normalize the sign, rat num positive, both num and denom positive
//rat num negative, only numerator is negative



function make_rat(n,d) {
  var g = gcd(n,d);
  if(n > 0){
    Math.abs(n,d);
  } else if (n < 0) {
    -Math.abs(n);
  }
   return pair(n / g, d / g);
}

function gcd(a,b) {
   return b === 0 ? a : gcd(b, a % b);
}
        

make_rat(6,-9);//-2,3
